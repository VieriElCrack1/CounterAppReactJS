
export const FirstApp = ( { title, subTitle } ) => {
    return (
        <>
            {/* <h1>{ newMessage.message }</h1> */}
            {/* <code>{ JSON.stringify(newMessage) }</code> */}
            <h1>{ title }</h1>
            <p>{ subTitle + 1 }</p>
        </>
    )
}
